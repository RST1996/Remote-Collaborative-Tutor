<%
	@apply = @course.requests.new(:user_id => current_user.id)
    if @apply.save 
    	flash[:notice] = 'Successfully applied' 
   else
    	flash[:alert] = 'Failed to apply'
   end
%>
	<script type="text/javascript">
		window.location.href="<%= root_path %>"	
	</script>
	